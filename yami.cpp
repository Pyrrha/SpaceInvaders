//
// Created by Pyrrha on 23/01/2016.
//

// /!\ Interface de lecture de fichier de config, ne pas toucher !!

#include <fstream>
#include <vector>

using namespace std;

/*vector <string> FileToVecStr (string Chemin_du_fichier)
{
    string str;
    vector <string> vec;
    ifstream input (Chemin_du_fichier);
    for (getline(input, str); !input.eof(); getline(input, str))
        vec.push_back(str);
    return vec;
}
*/